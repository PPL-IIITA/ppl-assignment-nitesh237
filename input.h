/**
 * CLASS INPUT
 * read the randomly generated data and stores them in appropriate data structure
 */
#include "couple.h"
class input {
	public:
	input(boy boys[], girl girls[], int *gt, int *bt); //!<read the randomly generated data and stores them in appropriate data structure
};