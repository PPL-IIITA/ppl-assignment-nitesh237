/**
 * CLASS generate_days
 * randomly generates days where the gift exchnage takes place
 */
class generate_days {
public:
	generate_days(int days[], int *t); //!<randomly generates days where the gift exchnage takes place
};